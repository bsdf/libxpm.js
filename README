render xpm images to the web

USEAGE:

    var render_options = {
        scale: 10,
        bg_checkered: true // make the transparent bg checkered
    };

    document.body.appendChild(libxpm.xpm_to_img(xpm_str, render_options));

TODO:
* rendering bugsz